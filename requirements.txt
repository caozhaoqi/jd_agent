pytest
pyttsx3
python-frontmatter
livekit
edge-tts
flask
openai
passlib
pyjwt
sqlmodel
tqdm
langchain-huggingface
sqlalchemy
bcrypt==3.2.2
langchain-text-splitters
# --- Web Framework (Web 框架) ---
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
# uvicorn[standard] 包含了 uvloop 和 httptools，性能更好

# --- LLM & LangChain Ecosystem (AI 核心库) ---
langchain>=0.1.0
langchain-core>=0.1.0
langchain-community>=0.0.10
langchain-openai>=0.0.5
# 如果你需要对接 DeepSeek，通常也是用 langchain-openai (兼容协议)

# --- Token Calculation (OpenAI 依赖的 Token 计算库) ---
tiktoken>=0.5.0

# --- Data Validation & Settings (数据验证与配置) ---
pydantic>=2.5.0
pydantic-settings>=2.1.0
# pydantic-settings 用于优雅地管理 .env 环境变量

# --- Utilities (通用工具) ---
python-dotenv>=1.0.0
httpx>=0.26.0
redis>=5.0.0
prometheus-client>=0.19.0
# httpx 是新一代的异步 HTTP 客户端，FastAPI 和 LangChain 底层常用
# redis 是 Redis 客户端库，用于缓存功能
# prometheus-client 用于监控指标收集

# --- Distributed Tracing (全链路监控) ---
opentelemetry-api>=1.22.0
opentelemetry-sdk>=1.22.0
opentelemetry-exporter-otlp-proto-grpc>=1.22.0
opentelemetry-instrumentation-fastapi>=0.43b0
opentelemetry-instrumentation-httpx>=0.43b0
opentelemetry-instrumentation-redis>=0.43b0
opentelemetry-instrumentation-sqlalchemy>=0.43b0
# opentelemetry-* 用于实现分布式追踪和全链路监控

# --- Machine Learning (机器学习) ---
pandas>=2.0.0
scikit-learn>=1.3.0
# pandas和scikit-learn用于机器学习缓存预测功能

# --- Logging (日志工具 - 对应 utils/logger.py) ---
loguru>=0.7.2
# 推荐使用 loguru 代替 Python 原生 logging，配置更简单，输出更美观

# --- Audio Transcoding (音频转码) ---
pydub>=0.25.1
# pydub用于音频格式转换，实现跨平台统一音频格式

# --- Video Processing (视频处理) ---
ffmpeg-python>=0.2.0
moviepy>=1.0.3
# ffmpeg-python和moviepy用于视频转音频功能

# --- Confluence Export (Confluence导出功能) ---
pdfkit>=1.0.0
# pdfkit用于HTML转PDF功能
atlassian-python-api>=3.0.0
# atlassian-python-api用于Confluence API访问
python-docx>=0.8.11
# python-docx用于Word文档生成
beautifulsoup4>=4.12.0
# beautifulsoup4用于HTML解析